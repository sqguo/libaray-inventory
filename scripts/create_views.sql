-- CREATE OR REPLACE VIEW BooksRatingsSummary (
--     bookID decimal(7),
--     rating decimal(3, 2),
--     totalRatings INT UNSIGNED NOT NULL DEFAULT 0,
-- ) AS SELECT
--     bookID,
--     SUM()
    
-- FROM BooksRatings;